name: Test

on:
  push:
    branches: ["**"]
    paths:
      - ".github/workflows/test.yml"
      - "extension/lib.js"
      - "backend/worker.js"
      - "test.mjs"
  pull_request:
    branches: ["**"]
    paths:
      - ".github/workflows/test.yml"
      - "extension/lib.js"
      - "backend/worker.js"
      - "test.mjs"

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "lts/*"

      - name: Run tests
        run: node test.mjs
